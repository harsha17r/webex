/**
 * Bundled illustration URLs (imported so Vite resolves + hashes on build).
 * Source file: `src/assets/illustrations/webex-test-meeting.png`
 */
export { default as webexTestMeetingIllustrationUrl } from '../assets/illustrations/webex-test-meeting.png'
